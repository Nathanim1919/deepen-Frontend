# Development environment variables for Vite.
# Files:
# - .env                (shared defaults)
# - .env.development    (development overrides)
# - .env.production     (production overrides)
# - .env.local          (local-only overrides, ignored by git)
#
# NOTE: Any variable exposed to the client MUST be prefixed with VITE_.
# Do NOT store secrets here (they are visible to the browser).

# Base URL for your API (ensure trailing slash for consistency)
VITE_API_BASE_URL=http://localhost:3000/api/v1/

VITE_AUTH_CALLBACK_URL=http://localhost:5173

# Optional explicit auth base; if unset, auth client will derive it as `${VITE_API_BASE_URL}/api/auth`
VITE_AUTH_BASE_URL=http://localhost:3000/api/auth

# Request timeout in milliseconds (string form)
VITE_API_TIMEOUT=10000

# Example feature flag or other env you might use during development
VITE_ENABLE_DEBUG=true
